apiVersion: apps/v1

kind: Deployment

metadata:

name: sampleapp

spec:

selector:

matchLabels:

run: sampleapp

replicas: 1

template:

metadata:

labels:

run: sampleapp

spec:

containers:

- name: glams

image: harishoo7/glams

ports:

- containerPort: 80

---

apiVersion: v1

kind: Service

metadata:

name: sampleapp

spec:

type: NodePort

selector:

run: glams

ports:

- port: 80

targetPort: 80
