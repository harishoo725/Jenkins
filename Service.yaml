apiVersion: v1
kind: Service
metadata: 
  name: harish
  
  namespace: dev
  
  
  labels:
    run: harish
spec:
  type: NodePort
  ports:
  - ports: 5000
    targetPort: 80
    protocol: TCP
    name: http
  
  selector:
    run: harish
